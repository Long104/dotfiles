return {
  'kevinhwang91/nvim-ufo',
  requires = 'kevinhwang91/promise-async',
  config = function()
    require('ufo').setup {
      fold = {
        enable = true,
        fold_method = 'expr',
        fold_expr = 'nvim_ufo#fold#foldexpr()',
      },
    }
  end,
}
